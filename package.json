{
  "name": "vite.net",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "p": "npm run build && npm run generate"
  },
  "dependencies": {
    "element-ui": "2.4.1",
    "nuxt": "latest"
  },
  "devDependencies": {
    "node-sass": "^4.9.0",
    "sass-loader": "^7.0.3"
  }
}
